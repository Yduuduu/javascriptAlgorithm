// 연필 개수

function solution(n) {
    // Math.ceil 올리기
    // Math.floor 내리기
    // Math.round 반올림
    // Math.sqrt 제곱근

    let answer=Math.ceil(n/12);
    return answer;
}

console.log(solution(178));